PORT=3000
ACCESS_KEY=change-me-to-a-long-random-string
MAIL_TRANSPORT=gmail-smtp

# Sender credentials are NOT stored here.
# Each user supplies their Gmail address, sender name, and App Password
# directly in the UI at send-time.

CHAPTER_NAME=IEEE Society on Social Implications of Technology (SSIT) at VIT Vellore
CHAPTER_SHORT_NAME=IEEE-SSIT
EVENT_NAME=Code4Change 4.0
SENDER_ROLE=Sponsorship Lead
CONTACT_PHONE=
MIN_RECIPIENTS=1
MAX_RECIPIENTS_PER_JOB=100
SEND_DELAY_MIN_MS=8000
SEND_DELAY_MAX_MS=20000
MAX_RETRIES=3
DAILY_SEND_LIMIT=400
